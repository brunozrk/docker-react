sudo: required
services:
  - docker

before_install:
  - docker build -t brunozrk/docker-react -f Dockerfile.dev .

script:
  - docker run -e "CI=true" brunozrk/docker-react npm run test -- --coverage
